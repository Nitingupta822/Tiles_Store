databases:
  - name: tiles-db
    plan: free

services:
  - type: web
    name: tiles-stock-app
    env: python
    buildCommand: cd tiles_stock_app && pip install -r requirements.txt
    startCommand: cd tiles_stock_app && gunicorn app:app
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: tiles-db
          property: connectionString
      - key: PYTHON_VERSION
        value: 3.11.0
